spring:
  application:
    name: poc-crud-api
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    jpa:
      hibernate:
        ddl-auto: update

app:
  security:
    jwt:
      secret: ${JWT_SECRET}
      expiration: 86400000
      issuer: products-api